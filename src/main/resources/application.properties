# Database Configuration
# Oracle Database connection settings for hospital schema

# JDBC URL - connects to FREEPDB1 pluggable database
db.url=jdbc:oracle:thin:@localhost:1521/FREEPDB1

# Database credentials
db.username=hospital
db.password=hospital123

# Connection pool settings (optional - for future use)
db.pool.minSize=5
db.pool.maxSize=20
db.pool.timeout=30000

# Application settings
app.name=Hospital Management System
app.version=1.0-SNAPSHOT

